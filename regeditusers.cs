using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Win32;
using System.Threading.Tasks;
using System.Collections;

namespace RegeditSample
{
    class regeditusers
    {
        public void goster()
        {
            //ArrayList a = new ArrayList();
            //foreach (object item in Registry.Users.GetSubKeyNames())
            //{
            //    a.Add(item);
            //}
            //foreach (string item in Registry.Users.OpenSubKey(a).ToString())
            //{
            //    a.Add(item);
            //}
            //Console.WriteLine(Registry.ClassesRoot.ToString());
            //foreach (object eleman1 in a)
            //    Console.WriteLine(eleman1);
            //Console.WriteLine(eleman1);
            //foreach (object eleman2 in Registry.ClassesRoot.GetSubKeyNames())
            //    Console.WriteLine(eleman2);     
        }
    }
}

